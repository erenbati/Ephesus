# Status snapshot — 2026-09-15

Read from `origin/main` @ `fe8d4a2` (2026-09-10). Working copy clean and exactly at
the ref. No open PRs. No previous snapshot existed; this is the first.

Evidence tiers: **A** ran it this session · **B** machine-checked independently ·
**C** repository fact (merge commit, file present, recorded verdict) · **D** prose
asserts it (rendered as *claimed*, never as done).

| Unit | Status | Tier | Note |
|---|---|---|---|
| M0 Skeleton | done | C | close-out audit 2026-08-26, verdict DONE |
| M1 One real agent, both planes | done | C | exit review 2026-08-26, verdict DONE |
| M2 Agora + Hermes | done | C | close-out audit 2026-08-27, audit fixes landed |
| M3 Artemis + the Watch | done | C | close-out audit 2026-08-27, audit fixes landed |
| M4 Library + engine breadth | done | C | close-out audit 2026-08-27, audit fixes landed |
| M5 The Odeon + Gymnasium v1 | done | C | close-out audit 2026-08-28, audit fixes landed |
| M5b The Stoa + company modes | done | C | close-out audit 2026-08-29, record amended |
| M6 Floor's face + the Herald | done | C | CLOSED 2026-08-30 against **amended** criteria; M6.9 excluded |
| M7 Harbor + outward missions | partial | C | M7.1–M7.7 done; **exit NOT met** — §6.1's reporting channel closed mid-run by defect #11 |
| M8 The company you can leave running | done | C | M8.0–M8.14 all ticked with evidence notes |
| M8b The crew can act | partial | C | 5/5 packages landed (docs 2026-09-09); **exit = the EXIT-M8 run, not performed** |
| M8c Bounded, honest about itself | partial | C | 10 planned + M8c.3b landed (docs 2026-09-09/10); **exit = the unattended hour, not performed** |
| M7b Recursive company + shipping | not started | C | 0/7 boxes; blocked behind the exit run |

## The gate everything converges on

`docs/EXIT-M8.md` — an unattended, non-author, clean-clone run inside a stated
ceiling — is the exit criterion of M8b **and** M8c, and M7b sits behind it. No
record of a completed run exists on `origin/main`; `docs/demo/` holds
`m8b-rehearsal-m8b-rehearsal.md` and nothing for M8c.

M8c.1 (a ceiling reachable without a mouse), M8c.2 (first ingest must not replay
history as news) and M8c.9 (a crew comes back after a restart) were built
specifically to make that run possible. With them landed, the run is unblocked
for the first time.

## Record gaps (evidence exists, the record is silent)

1. ~~**`docs/PROGRESS.md` never mentions M8b or M8c.**~~ **CLOSED the same day.** Its
   last entry was 2026-09-06 and 16 packages had landed since; it self-describes as
   the resume point, so a session resuming from it would have restarted at M6.9 or
   M7b. Both sections were written from the sixteen implementation docs — see
   [the record](../implementations/2026-09-15-progress-record-and-engineer-skill.md).
   The two exit boxes stay **unticked**, because the run they name has not happened.
   Closing this also turned `check-readme-current.cjs` red and required the README's
   landed list and narrative to catch up, which is now done.
2. **M6.9 reads DEFERRED INDEFINITELY but is implemented and unmerged** —
   `feature/m6-9-severity-1-announcements`, 22 files, +1229 −59, ADR-0032 and an
   implementation doc, dated 2026-09-07. Nothing named `announcement` is on main.
3. **Four Gymnasium metrics came due 2026-09-11 with the Outcome column empty** —
   GYM-002, GYM-003, GYM-004, GYM-005. GYM-006 is due 2026-09-16.
4. **GYM-007 reads `proposed`** with no decision date, yet corroboration logic is
   present in `scripts/check-coverage.cjs` on main.

## Evidence gaps (record asserts, evidence absent or contrary)

- M8c exit: no run artifact of any kind. The recollection that the last attempt
  aborted 21 s in is tier D.
- M8b exit: same run; the rehearsal produced M8c.9 and M8c.10, not a pass.
- M7 exit: the project's own 2026-09-06 entry records it as NOT closed, with
  defect #11 (Harbor's `awaiting` map does not survive a restart) open.

## Other unmerged branches checked

| Branch | Verdict |
|---|---|
| `fix/coverage-ratchet-corroboration` (2026-09-04) | content appears present on main; likely superseded |
| `fix/floor-canvas-deterministic-coverage` (2026-09-04) | unmerged, overlaps the above |
| `docs/real-activation-run` (2026-09-05) | one doc, 147 lines, unmerged |

## Scope limits on everything above

One platform (win32), one machine. Nothing was executed this session — no suite,
build or coverage run. Every row rests on merge commits, implementation docs and
the project's own recorded verdicts.
